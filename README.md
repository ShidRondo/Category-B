# Category-B
The purpose of this project is for practical Examination for Set-B

# If Python is not installed
# Run this in the Codespaces terminal:
sudo apt update sudo apt install python3 -y

# Check if Python is installed:
python3 --version

# Install SQLITE3
sudo apt update sudo apt install sqlite3 -y

# Check SQLite3
sqlite3 --version

Structured Data ETL Practical Exam
Topic: Load structured CSV data into SQLite3 using Python

Student Task:
Complete only the SQLite3 parts:

1. CREATE TABLE
2. INSERT INTO
3. SELECT FROM

The extraction and transformation code is already provided.

